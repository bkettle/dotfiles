- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.config/nvim:
        create: true
        path: nvim
    ~/.zshrc:
        path: zshrc
    ~/.tmux.conf:
        path: tmux
    ~/.config/git/config:
        create: true
        path: gitconfig
    ~/.config/alacritty:
    ~/.config/yabai/yabairc:
        create: true
        path: yabairc
    ~/.config/skhd:
        create: true
        path: skhdrc


- shell:
  - [git submodule update --init --recursive, Installing submodules]
